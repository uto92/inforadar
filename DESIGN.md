# 構想: 来場チェックインの再設計

3つの拡張要望（場所の記録・多様なIDの受け入れ・個人による自己チェックインへの
「民主化」）を、場当たりの機能追加ではなく1つの再定義として整理する。

> 関連プロジェクトとの境界: ICカード履歴から行動を把握するのは別プロジェクト
> （transit-collector）。本プロジェクトは来場の事実だけを扱い、ソルトを共有しない
> （`server/NFC_PROBE.md` 参照）。この境界は本構想でも維持する。

---

## 1. 再定義: チェックインとは (誰が, どこに, いつ) の3つ組

現在の記録単位は (仮名, イベント, 時刻)。「イベント」は実質「場所＋日付」を
兼ねた入れ物になっている。これを分解する:

```
チェックイン = ( 誰が , どこに , いつ )
                 │       │
                 │       └─ 場所（place）を第一級の概念に昇格
                 └─ 身元は「階段」— 強さの異なる複数の方式を等価に扱う
```

イベントは廃止しない。当面は「場所の集まり＋期間」の管理単位として残し、
将来は集計ビュー（クエリ）へ退化させる余地を残す。

すべての拡張は「誰が」「どこに」の**取り方のバリエーション**として説明できる。
これが本構想の背骨。

## 2. 拡張1: 場所 — 「誰がどこに来たか」

### モデル

- `wc_places` (id, name, …) を追加。checkin に `place_id`（NULL許容）を追加
- 既存データは place_id NULL のまま有効（後方互換）

### 取り方A: スタッフ端末を場所に紐づける（有人受付）

現行の「受付を増やす」QRを拡張し、場所IDを含める:

```
#/join?e=<eventId>&s=<salt>&p=<placeId>&...
```

スキャナーはQRを読んだ時点で「この端末は○○ブースの受付」になり、
以後の読取すべてに場所が刻印される。現行の仕組みの自然な延長で、
スタッフの操作は何も増えない。

### 取り方B: 場所にQRを掲示する（無人・来場者操作）→ 拡張3へ接続

場所に貼られたQRを**来場者自身のスマホ**が読む。これが民主化の入口になる。

### ⚠ 最重要の設計制約: 公開QRにソルトを入れない

QRは2種類あり、**絶対に混同してはならない**:

| | 含むもの | 扱い |
|---|---|---|
| スキャナー設定QR（現行join） | イベントID＋**ソルト** | 秘密。スタッフ間でのみ受け渡し |
| 来場者向け場所QR（新設） | 公開place tokenのみ | 公開。壁に貼る |

現行のjoin QRをそのまま壁に貼ると**ソルトが公開**され、記録済みの全仮名が
総当たりで逆引き可能になる（12桁は全探索できる）。来場者向けQRは
ソルトを含まない別系統として設計する。これを崩す実装は他の何が正しくても不合格。

## 3. 拡張2: 多様なID — 現行方針の維持と「身元の階段」

「発行元を問わず任意のカードを受け付ける」方針は維持する（実装済み）。
その上で、身元の取り方を強さの異なる階段として整理する:

| 段 | 方式 | 強さ | 使いどころ |
|---|---|---|---|
| 1 | カード仮名（スタッフが読取） | 中: カードを持つ人 | 有人受付（現行） |
| 2 | 端末仮名（ブラウザに保存した乱数） | 弱: 同じブラウザ | 無人の自己チェックイン初期 |
| 3 | パスキー（WebAuthn） | 強: 生体認証つき本人 | 自己ログイン・マイページ |

どの段で取っても記録の形は同じ (仮名, 場所, 時刻)。段が違えば仮名の
名前空間を分ける（`:nfc:` と同じ手法）。集計上は「別の人」として数え、
本人の意思による連携（§4）でのみ結合する。

## 4. 拡張3: 民主化 — アプリ不要の自己チェックインとマイページ

### 4-1. 自己チェックイン（アプリレス）

```
壁のQRを自分のスマホで読む → ブラウザでページが開く
  → 目的の説明と同意 → 「チェックインする」 → 記録
```

- インストール不要（Web）。ログインも初期は不要（端末仮名＝段2）
- 同意が構造的に改善する: スタッフに読まれるのではなく、本人が目的を読み、
  本人がボタンを押す

### 4-2. 自己ログイン（パスキー）とマイページ

「民主化」の本丸はチェックイン手段ではなく**データへのアクセス**だと捉える:

- パスキーで登録・ログイン（生体認証。パスワードもアプリも不要）
- マイページで**自分の来場履歴を自分で見られる**
- 削除請求もここから自己処理できる余地

収集側だけがデータを見られる非対称を崩すこと自体が、この構想の
「イノベーティブ」の中身だと位置づける。

### 4-3. ID連携（任意・本人操作のみ）

普段は有人受付でカード、たまに自己チェックイン、という人は2つの仮名に
分裂する。連携したい本人だけが、有人受付でログイン済み画面を提示して
カードを読んでもらい、2仮名を接続する（`wc_links`）。本人の操作なしに
システム側で推測結合はしない。

### 4-4. 明示的な意思決定が必要な項目: カードの自己スキャン

来場者が自分のカードを自分のスマホで読む案は技術的には可能（読取UIは
流用できる）が、**仮名の算出にソルトが要る**ため、公開ページに置けない。
成立させるには生値をHTTPSでWorkerへ送り、サーバ側の秘密ソルトで
ハッシュ化する構成になる。これは「生値はサーバを通らない」という現在の
性質の放棄であり、やるなら明示的に決めてから。当面は段2・段3で代替する。

## 5. アーキテクチャ

```
スタッフ面（現行）                     公開面（新設）
visit-checkin.workers.dev             visit-self.workers.dev（仮）
  Cloudflare Access で保護              認証なし・Turnstile＋レート制限
  受付スキャナー / 管理 / 掲示文          場所QRの着地ページ / マイページ
        │                                   │
        └──────────── 同じ D1 ──────────────┘
```

- **公開面はWorkerを分ける。** 現行WorkerはAccessで全体が守られており、
  公開ページを同居させると保護の切り分けが複雑になる。分ければ
  事故時の影響範囲も分離される
- 公開POSTには Cloudflare Turnstile（無料・アプリレス）とレート制限
- スキーマ追加案: `wc_places` / `wc_checkins.place_id` /
  （段3で）`wc_identities`, `wc_links`

## 6. 段階計画

| Phase | 内容 | 状態 |
|---|---|---|
| 1 | 場所モデル＋スタッフ端末の場所紐づけ（取り方A） | **実装済み**（migrations/0004, PlacesCard, join QRのp） |
| 2 | 公開Worker＋場所QR＋自己チェックイン（端末仮名） | **実装済み**（server/src/self.ts = visit-self Worker） |
| 3 | パスキー＋マイページ＋ID連携 | 未着手 |
| 保留 | カード自己スキャン | 未決（サーバ側ハッシュ化の可否 §4-4） |

Phase 1 は既存の延長で小さい。Phase 2 から「公開面を持つ」という
性質の変化が入る。

## 7. 既知の限界（実証段階では受容し、明記する）

- **静的QRの写真リプレイ**: 場所QRを撮影すれば来場せずにチェックイン可能。
  実証では受容。対策が要る段階になったら時刻コード入りの動的QR表示へ
- **端末仮名の弱さ**: ブラウザデータ削除・端末変更で別人になる。
  パスキー（段3）が恒久解
- **自己申告性**: 自己チェックインは「そこに居た」の証明ではなく申告。
  厳密性が要る用途には有人受付を使い分ける
