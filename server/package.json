{
  "name": "wester-visit-scanner-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test:unit": "node --test --experimental-strip-types test/*.test.ts",
    "migrate:local": "wrangler d1 migrations apply wester-visit-scanner --local",
    "migrate:remote": "wrangler d1 migrations apply wester-visit-scanner --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
