# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`inforadar`) is currently a greenfield project. It contains only a README.md with the project name — no source code, build system, tests, or tooling configuration exist yet.

## Working in This Repository

- There are no build, lint, or test commands to run yet. Do not assume a language, framework, or package manager until one is established in the codebase.
- When the initial project scaffolding is added (e.g., a `package.json`, `pyproject.toml`, `go.mod`, or similar), update this file with:
  - The commands to build, lint, and run tests (including how to run a single test).
  - A high-level description of the architecture as it takes shape.
